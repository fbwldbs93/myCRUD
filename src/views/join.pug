extends base

block content 
    form(method="POST")
        input(type="text" name="name" placeholder="insert ID")
        input(type="text" name="password" placeholder="insert Password")
        input(type="submit" value="Join")